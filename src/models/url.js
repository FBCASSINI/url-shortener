/*
// Create---------------------------------------------------------------------------//
exports.create = (payload, err, success) => {
  url.create(payload).then(success).catch(err);
};

// READ----------------------------------------------------------------------------//
exports.findAll = (err, success) => {
  url.findAll().then(success).catch(err);
};

*/
